<%= form_for [@tank] do |f| %>
  <%= f.label :id, "Tank" %>
  <%= f.select(:id, options_for_select(@tanks_select)) %><br>
  <%= f.hidden_field(:brewery_id, :value => current_user.brewery.id) %>
  <%= f.submit("Remove Tank") %>
<% end %>
