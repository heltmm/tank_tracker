$('#tank<%=@start_tank.id%>').html("<%= j render 'tank', :tank => @start_tank %>");
$('#tank<%=@finish_tank.id%>').html("<%= j render 'tank', :tank => @finish_tank %>");
$('#transfer_update_form')[0].reset()
